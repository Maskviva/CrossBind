[package]
name = "bedrock-protocol"
version = "0.2.0"
edition = "2021"
license = "Apache-2.0"
description = "Cross-version Bedrock protocol translation: packet ids, version graph, translators"

[dependencies]
bedrock-codec = { path = "../bedrock-codec" }
